services:
  # API Django Backend
  - type: web
    name: tp-redis-api
    runtime: docker
    dockerfilePath: ./api/Dockerfile
    dockerContext: ./api
    plan: free
    envVars:
      - key: REDIS_URL
        fromService:
          type: redis
          name: tp-redis-cache
          property: connectionString
      - key: DATABASE_URL
        fromDatabase:
          name: tp-redis-db
          property: connectionString
      - key: DEBUG
        value: "False"

  # Frontend React
  - type: web
    name: tp-redis-web
    runtime: docker
    dockerfilePath: ./web/Dockerfile
    dockerContext: ./web
    plan: free

  # Cache Redis  
  - type: redis
    name: tp-redis-cache
    plan: free
    ipAllowList: []

databases:
  - name: tp-redis-db
    plan: free